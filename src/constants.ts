export enum ALIGNMENT {
    AUTO = 'auto',
    START = 'start',
    CENTER = 'center',
    END = 'end',
}

export enum DIRECTION {
    HORIZONTAL = 'horizontal',
    VERTICAL = 'vertical',
}

export enum SCROLL_CHANGE_REASON {
    OBSERVED = 'observed',
    REQUESTED = 'requested',
}
